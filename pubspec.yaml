name: sokrio_task_flutter
description: 'A new Flutter project.'
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  #iOS Icons
  cupertino_icons: ^1.0.8
  # Dependency Injection
  get_it: ^8.2.0
  #Functional Programming
  dartz: ^0.10.1
  # State Management
  flutter_bloc: ^9.1.1
  # Network Status
  connectivity_plus: ^7.0.0
  #Network Calling
  dio: ^5.9.0
  shared_preferences: ^2.5.3
  # Cache Management
  image_picker: ^1.2.0
  # Image Picker
  http_parser: ^4.1.2
  # Parsing and serializing various HTTP-related formats
  intl: ^0.20.2
  # Shimmer Loader
  shimmer: ^3.0.0
  # Debug Beautifier
  ansicolor: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
